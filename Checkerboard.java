public class Checkerboard
{
	private int size;
	//size is an integer variable only privately viewed accessible only in this class
	public CheckerBoard(int x)
	{
		this.size = x; //Checkerboard of size SIZE is constructed
}
	public int getSize()
	{
		return this.size;
}
}